<%= form_for(articulo) do |f| %>
  <% if articulo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(articulo.errors.count, "error") %> prohibited this articulo from being saved:</h2>

      <ul>
      <% articulo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :codigo_articulo %>
    <%= f.text_field :codigo_articulo %>
  </div>

  <div class="form-group">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="form-group">
    <%= f.label :unidad_compra_diaria %>
    <%= f.text_field :unidad_compra_diaria %>
  </div>

    <div class="form-group">
      <%= f.label :total_inventario %>
      <%= f.text_field :total_inventario %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
